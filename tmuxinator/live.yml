# ~/.tmuxinator/live.yml

name: live
root: ~/code/screenshot-streamer

windows:
  - editor: vim
  - console: rails c
  - git:
  - server: rails s
  - locallogs:
      layout: even-horizontal
      panes:
        - tail -f log/development.log
        - tail -f log/test.log
