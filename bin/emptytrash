#!/bin/bash
#
# Script to empty the trash from command line.
# Run on startup (.xinitrc), via cron, etc

trash="/home/$USER/.local/share/Trash"

rm -rdf $trash/info/*
rm -rdf $trash/files/*

# Remove .files without . and .. directories
rm -rdf $trash/info/.[^.]*
rm -rdf $trash/files/.[^.]*







