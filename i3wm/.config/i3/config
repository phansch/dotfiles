# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1

font pango:Ubuntu Mono derivative Powerline 10

# class                 border  backgr. text    indicator child_border
client.focused          #282828 #98971a #282828 #ffffff   #282828

# Never show borders (exceptions below)
new_window none

# Hide borders that are adjecent to screen edges
hide_edge_borders none

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Cycle between workspaces
workspace_auto_back_and_forth yes

# Keybindings

## reload the configuration file
bindsym $mod+Shift+c reload

## restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# Exit i3
bindsym $mod+Shift+q exit

# kill focused window
bindsym $mod+Shift+e kill

# change focus
# bindsym $mod+o focus left
# bindsym $mod+j focus down
# bindsym $mod+k focus u
# bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+J move left
bindsym $mod+Shift+K move down
bindsym $mod+Shift+L move up
bindsym $mod+Shift+O move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+h split h

# split in vertical orientation
bindsym $mod+Shift+v split v

# enter fullscreen mode for the focused container
# bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

bindsym $mod+x move workspace to output right

# Scratchpad configuration
bindsym $mod+minus [instance="urxvt" title="vimwiki"] scratchpad show
for_window [instance="urxvt" title="vimwiki"] move scratchpad
exec_always --no-startup-id urxvt -title "scratchpad" -e ~/.bin/scratchpad_start

# ASSIGN WORKSPACE VARIABLES
set $WS1 1:dev
set $WS2 2:www
set $WS3 3:im
set $WS4 4:media
set $WS5 5:misc
set $WS6 6:misc
set $WS7 7:misc
set $WS8 8:misc

# switch to workspace
bindsym $mod+1 workspace $WS1
bindsym $mod+2 workspace $WS2
bindsym $mod+3 workspace $WS3
bindsym $mod+4 workspace $WS4
bindsym $mod+5 workspace $WS5
bindsym $mod+6 workspace $WS6
bindsym $mod+7 workspace $WS7
bindsym $mod+8 workspace $WS8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+exclam move container to workspace $WS1
bindsym $mod+Shift+quotedbl move container to workspace $WS2
bindsym $mod+Shift+section move container to workspace $WS3
bindsym $mod+Shift+dollar move container to workspace $WS4
bindsym $mod+Shift+percent move container to workspace $WS5
bindsym $mod+Shift+ampersand move container to workspace $WS6
bindsym $mod+Shift+slash move container to workspace $WS7
bindsym $mod+Shift+parenleft move container to workspace $WS8
bindsym $mod+Shift+parenright move container to workspace 9
bindsym $mod+Shift+equal move container to workspace 10

# window specific settings
## Move to workspace
### Note that everything that's using urxvt as a base,
### must be assigned before urxvt itself
assign [title="irssi"] $WS3
assign [instance="urxvt"] $WS1
assign [class="^Firefox$"] $WS2
assign [class="qutebrowser"] $WS2
assign [class="Skype*"] $WS3
assign [class="Pidgin"] $WS3
assign [title="Messenger"] $WS3
assign [class="Hexchat"] $WS3
# assign doesn't work with Spotify because it doesn't set the
# window classes early enough, so we have to use for_window:
# https://github.com/i3/i3/issues/2060
for_window [class="spotify|Spotify"] move to workspace $WS4
for_window [title="spotify|Spotify"] move to workspace $WS4
# Same issue with hipchat
for_window [class="hipchat4"] move to workspace $WS3
assign [class="VirtualBox"] $WS5
assign [class="eepassx"] $WS5
for_window [class="deja-dup|Deja-dup"] move to workspace $WS6; workspace $WS6
for_window [title="Auto-Type - KeePassX"] move to workspace current
assign [title="Icedove|Thunderbird"] $WS7

## window floating, borders and positioning
for_window [class="^Firefox$" title="^Library$"] floating enable
for_window [class="^Firefox$" window_role="^Preferences$"] floating enable
for_window [class="^Anki$"] floating enable, border normal, move absolute position center
for_window [class="^Skype$"] floating enable
for_window [title="xfce4-notifyd|Volume Control"] floating enable
for_window [class="^Shutter$"] floating enable, border normal
for_window [class="Thunar"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [class="xfce4-notifyd"] floating enable
for_window [class="deja-dup|Deja-dup"] floating enable
for_window [class="eepassx"] floating enable, border none, move absolute position center
for_window [title="spotify|Spotify"] floating enable, border none, move absolute position center
for_window [title="Bluetooth"] floating enable, border none, move absolute position center
for_window [class="ile-roller$"] floating enable, border none, move absolute position center

# Autostart
# Use --no-startup-id for background programs or if there is a 'watch' cursor
# exec --no-startup-id xrandr --output LVDS1 --mode 1366x768 --output HDMI1 --mode 1920x1080 --right-of LVDS1
exec firefox
exec qutebrowser --backend webengine
exec spotify
exec keepassx2
exec xbindkeys

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Colors from ~/.Xresources

set_from_resource $background background #ff0000
set_from_resource $foreground foreground #ff0000
set_from_resource $warning color9 #ff0000

# Start i3bar to display a workspace bar
bar {
  output LVDS1
  status_command i3status
  position top
  modifier $mod
  mode hide
  # separator_symbol "•"

  colors {
            background $background
            statusline $foreground

            #                 border background text
            focused_workspace $background $foreground $background
            active_workspace $background $background $foreground
            inactive_workspace $background $background $foreground
            urgent_workspace $background $warning $foreground
  }
}
bar {
  status_command i3status
  position top
  modifier $mod
  mode hide
  # separator_symbol "•"

  colors {
            background $background
            statusline $foreground

            #                 border background text
            focused_workspace $background $foreground $background
            active_workspace $background $background $foreground
            inactive_workspace $background $background $foreground
            urgent_workspace $background $warning $foreground
  }
}
