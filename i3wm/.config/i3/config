# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

set $border #f1fa8c
set $background #282a36
set $foreground #bd93f9
set $warning #ff5555

# Use the default monospace font defined via fontconfig
font pango:monospace, FontAwesome

# class                 border  backgr. text    indicator child_border
client.focused          $foreground $foreground $background #ffffff   $border
client.focused_inactive $border $background $foreground #ffffff   $border
client.unfocused        $background $background $foreground #ffffff   $border
client.background       #ffffff

default_border pixel 5

# Hide borders that are adjecent to screen edges
hide_edge_borders both

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Cycle between workspaces
workspace_auto_back_and_forth yes

title_align center

# Keybindings

## reload the configuration file
bindsym $mod+Shift+c reload

## restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# Exit i3
bindsym $mod+Shift+q exit

# kill focused window
bindsym $mod+Shift+e kill

# change focus
# bindsym $mod+o focus left
# bindsym $mod+j focus down
# bindsym $mod+k focus u
# bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+J move left
bindsym $mod+Shift+K move down
bindsym $mod+Shift+L move up
bindsym $mod+Shift+O move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+h split h

# split in vertical orientation
bindsym $mod+Shift+v split v

# enter fullscreen mode for the focused container
# bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+Shift+S layout stacking
bindsym $mod+Shift+W layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

bindsym $mod+x move workspace to output left

# ASSIGN WORKSPACE VARIABLES
set $WS1 1:dev
set $WS2 2:www
set $WS3 3:notes
set $WS4 4:media
set $WS5 5:misc
set $WS6 6:misc
set $WS7 7:misc
set $WS8 8:misc
set $WS9 9:im

# switch to workspace
bindsym $mod+1 workspace $WS1
bindsym $mod+2 workspace $WS2
bindsym $mod+3 workspace $WS3
bindsym $mod+4 workspace $WS4
bindsym $mod+5 workspace $WS5
bindsym $mod+6 workspace $WS6
bindsym $mod+7 workspace $WS7
bindsym $mod+8 workspace $WS8
bindsym $mod+9 workspace $WS9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $WS1
bindsym $mod+Shift+2 move container to workspace $WS2
bindsym $mod+Shift+3 move container to workspace $WS3
bindsym $mod+Shift+4 move container to workspace $WS4
bindsym $mod+Shift+5 move container to workspace $WS5
bindsym $mod+Shift+6 move container to workspace $WS6
bindsym $mod+Shift+7 move container to workspace $WS7
bindsym $mod+Shift+8 move container to workspace $WS8
bindsym $mod+Shift+9 move container to workspace $WS9
bindsym $mod+Shift+0 move container to workspace 10

# window specific settings
## Move to workspace
assign [class="^Firefox|Nightly$"] $WS2
assign [class="^Anki$"] $WS3
assign [class="obsidian"] $WS3
assign [class="Slack"] $WS9
assign [class="vlc"] $WS4
assign [title="pulsemixer"] $WS4
# Ghostty has to come last, because otherwise it would move pulsemixer to
# WS1 as well
assign [class="ghostty"] $WS1

## window floating, borders and positioning
for_window [class="^Firefox|Nightly$" title="^Library$"] floating enable
for_window [class="^Firefox|Nightly$" window_role="^Preferences$"] floating enable
for_window [class="^Anki$"] floating enable, border normal, move absolute position center
for_window [title="xfce4-notifyd|Volume Control"] floating enable
for_window [class="Thunar"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [class="xfce4-notifyd"] floating enable
for_window [title="Bluetooth"] floating enable, border none, move absolute position center
for_window [class="ile-roller$"] floating enable, border none, move absolute position center
for_window [title="pulsemixer"] floating enable, border none, move absolute position center
for_window [class="vlc"] floating enable, move absolute position center

# Autostart
# Use --no-startup-id for background programs or if there is a 'watch' cursor
# exec --no-startup-id xrandr --output LVDS1 --mode 1366x768 --output HDMI1 --mode 1920x1080 --right-of LVDS1
exec /home/phansch/.bin/startup

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar
bar {
  status_command ~/.cargo/bin/i3status-rs ~/.config/i3status-rust/config.toml
  position bottom
  modifier $mod
  mode dock
  tray_output primary

  colors {
            background $background
            statusline $foreground

            #                 border background text
            focused_workspace $background $foreground $background
            active_workspace $background $background $foreground
            inactive_workspace $background $background $foreground
            urgent_workspace $background $warning $foreground
  }
}
